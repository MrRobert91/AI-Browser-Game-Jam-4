# Optional post-jam variant. Leave empty to preserve the fully offline build.
# The browser never receives a model-provider key; point only to an HTTPS proxy
# that implements the retention and cost policy in docs/privacy/REMOTE_HAIKU.md.
VITE_REMOTE_HAIKU_ENDPOINT=
